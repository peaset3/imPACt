<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xreference="clr-namespace:Xamarin.Forms.Xaml"
             mc:Ignorable="d"
             x:Name="connectionspage"
             x:Class="imPACt.Views.ConnectionsPage"
             Title="Connections"
             IconImageSource="link.png">
    <ContentPage.Content>
        <StackLayout x:Name="sl1">
            
            <Entry x:Name="viewprofile" Placeholder="Search for someone" Text="{Binding Query}" />
            <Button x:Name="requestprofilebtn" Text="Search" BackgroundColor="#3897F0" TextColor="White" WidthRequest="150" HorizontalOptions="Center" Command="{Binding GotoProfileCommand}" />


            <ListView x:Name="connectionslist" ItemsSource="{Binding Connections}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Fullname}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <!--<Label Text="Current and Pending Connections"></Label>
            <ListView x:Name="ConnectionView" ItemsSource="{Binding Connections}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding Fullname}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->

            <Label Text="Suggested Connections"/>
            <ListView x:Name="SuggestedConnections" ItemsSource="{Binding PotentialConnections}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="1*" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Grid.Column="0" Source="{Binding PhotoSource}" HeightRequest="50" WidthRequest="50"/>
                                    <Label Grid.Column="1" Text="{Binding Fullname}" HorizontalTextAlignment="Start" VerticalTextAlignment="Center"/>
                                    <Button Grid.Column="2" Text="View" Command="{Binding Path=BindingContext.GotoConnectionCommand, Source= {x:Reference SuggestedConnections}}" CommandParameter="{Binding Uid}"/>

                                </Grid>
                            
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

         
            
            <Entry x:Name="requestconn" Placeholder="Request connection by email" Text="{Binding RequestEmail}" />
            <Button x:Name="requestbtn" Text="Add Connection" BackgroundColor="#3897F0" TextColor="White" WidthRequest="150" HorizontalOptions="Center" Command="{Binding AddConnectionCommand}" />
        
        </StackLayout>
    </ContentPage.Content>
</ContentPage>